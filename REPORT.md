# Отчёт по лабораторной работе №2
## Управление конкурентными транзакциями в маркетплейсе

**Студент:** _Ирина Ромадова_  
**Дата:** _27.02.2026_

---

## Раздел 1: Описание проблемы

### Что такое Race Condition?

_TODO: Объясните, что такое race condition в контексте баз данных._

Race condition (состояние гонки) — это ситуация, когда порядок выполнения влияет на результат программы. В контексте БД это касается чтения и записи общего ресурса.

**Пример из жизни:**
- Если два человека пытаются одновременно купить билет на одно и то же место на концерте и отсутствуют мехинизмы блокировок. Может получиться так, что билет продан дважды

### Почему READ COMMITTED не защищает от двойной оплаты?

_TODO: Объясните механизм работы READ COMMITTED и почему он не предотвращает race condition._

На уровне изоляции READ COMMITTED:
1. Каждая транзакция видит, только то что было закомиченно другими транзакциями до него
2. Когда две транзакции одновременно хотят провести оплату, обе видят статус "создан". И...
3. В результате может произойти двойная оплата, так каждая транзакция увидела, что ей оплату провести можно.
То есть тут чтобы не было такого одна транзакция должна завершиться (коммит)

**Демонстрация проблемы:**

```
Время | Сессия 1                    | Сессия 2
------|----------------------------|---------------------------
t1    | BEGIN                      |
t2    | SELECT status (created)    |
t3    |                            | BEGIN
t4    |                            | SELECT status (created)
t5    | pg_sleep(3)                |
t6    |                            | pg_sleep(3)
t7    | UPDATE status = paid       |
t8    |                            | UPDATE status = paid (ждет)
t9    | COMMIT                     |
t10   |                            | UPDATE выполняется...
t11   |                            | COMMIT
```

t1-t2 - сессия один начинается, видит статус created
t3 -t4 сессия два начинается, видит что статус заказа создан

итак, обе сессии увидели одно и то же

t5, t6 - обе транзакции "спят", чтобы выполнились другие операции (имитация этого действия).

вот тут каждая из транзакций решила что ей можно установить статус оплачен, потому что на момент селекта не было изменений в этом заказе

t7 - первая сессия ставит статус paid (трназакция еще не все). Здесь происходит блокировка

t8 - вторая ждет пока снимется блокировка

t9 - первая сессия делает коммит, транзакция завершается. В истории у заказа есть оплата. Блокировка снята
t10- вторая дождалась снятия блокировки. тоже сделала статус paid (не узнала что тут уже стоит статус paid, потому что у нее данные на момент ее селекта из t4)
t11 - конец второй транзакции и у заказа вышла вторая оплата.

### Примеры из реальной жизни

_TODO: Приведите реальные примеры, когда может возникнуть такая проблема._

1. **Двойной клик на кнопку "Оплатить"**
   - Пользователь нажимает кнопку дважды
   - Два HTTP-запроса приходят почти одновременно
   - Оба запроса начинают обработку параллельно

2. **Микросервисная архитектура**
   - Несколько сервисов обрабатывают одно и то же событие
   - Если примерно в одно время начали и обратились к одной записи, могут повторить одно действие дважды тк видят старое состояние ресурса.

3. **Сбой сети**
   - Отправлена оплата, но из за сбоя сети нет ответа сразу
   - Пользователь снова платит
   - Оплата снова обработается если нет защиты от повтороной обработки

---

## Раздел 2: Уровни изоляции в PostgreSQL

### READ UNCOMMITTED

**Описание:**
Самый слабый уровень изоляции. Может читать то что было изменено другой транзакцией

**Предотвращает:**
- ничего)

**Не предотвращает:**
- Dirty reads
- Non-repeatable reads
- Phantom reads

**Когда использовать:**
- никогда.
- НУ или если очень важна максимальная скорость

**Особенность в PostgreSQL:**
В PostgreSQL READ UNCOMMITTED работает как READ COMMITTED из-за архитектуры MVCC.

---

### READ COMMITTED (по умолчанию)

**Описание:**
Читает только то, что было зафиксировано другими транзакциями. Незакомиченные - нет.

Каждый SELECT в транзакции видит актуальное зафиксированное состояние другими трназкциями. Незакомиченные изменения не читаются

**Предотвращает:**
- ✅ Dirty reads (чтение незакоммиченных данных)

**Не предотвращает:**
- ❌ Non-repeatable reads (повторное чтение дает другой результат)
- ❌ Phantom reads (новые строки появляются в результате запроса)

**Пример non-repeatable read:**
```sql
-- Сессия 1
BEGIN;
SELECT balance FROM accounts WHERE id = 1; -- Результат: 1000
-- Сессия 2 изменяет balance и делает COMMIT
SELECT balance FROM accounts WHERE id = 1; -- Результат: 500 (!)
COMMIT;
```

**Когда использовать:**
- чтение данных
- когда целостность данных не важна 

---

### REPEATABLE READ

**Описание:**
Транзакция локально фиксирует состояние данных на момент начала выполнения транзакции (это нахывается снапшот). Потом все селекты возвращают одно и то же, даже если параллельные операции поменли состояния

Транзакция видит snapshot данных на момент начала...

**Предотвращает:**
- ✅ Dirty reads
- ✅ Non-repeatable reads
- ✅ Phantom reads (в PostgreSQL благодаря MVCC)

**Не предотвращает:**
- ❌ Write skew (специфичная аномалия)
- ❌ Некоторые сериализационные аномалии

**Особенность в PostgreSQL:**
В отличие от стандарта SQL, PostgreSQL на REPEATABLE READ также предотвращает phantom reads.

**Когда использовать:**
- в рамках транзацкии все данные были одинаковы (резервирование товаров, заказов, средств и тп)

---

### SERIALIZABLE

**Описание:**
Результаты параллельного выполнения транзакций должны быть такими же как и при последовательном исполнении.

Самый строгий уровень, гарантирующий...

**Предотвращает:**
- ✅ Все аномалии чтения
- ✅ Сериализационные аномалии
- ✅ Write skew

**Недостатки:**
- ❌ Может откатывать транзакции при конфликтах (serialization failure)
- ❌ Снижение производительности
- ❌ Требует retry logic в приложении

**Пример serialization failure:**
```sql
-- Сессия 1
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 100 WHERE id = 1;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts; -- 5000
UPDATE accounts SET balance = balance + 200 WHERE id = 2;

-- При COMMIT одна из транзакций получит ошибку:
-- ERROR: could not serialize access due to read/write dependencies
```

**Когда использовать:**
- банковские переводы, бронирование. Системы, где корректность важнее скорости и тп.
---

### Сравнительная таблица

| Уровень изоляции  | Dirty Read | Non-Repeatable Read | Phantom Read | Performance | Use Case |
|-------------------|------------|---------------------|--------------|-------------|----------|
| READ UNCOMMITTED  | ❌          | ❌                   | ❌            | Высокая     | Аналитика (неточная) |
| READ COMMITTED    | ✅          | ❌                   | ❌            | Высокая     | Обычные операции |
| REPEATABLE READ   | ✅          | ✅                   | ✅*           | Средняя     | Критичные операции |
| SERIALIZABLE      | ✅          | ✅                   | ✅            | Низкая      | Финансовые транзакции |

_*В PostgreSQL REPEATABLE READ также предотвращает phantom reads благодаря MVCC._

---

## Раздел 3: Решение проблемы

### Почему REPEATABLE READ решает проблему?

_TODO: Объясните, как REPEATABLE READ помогает решить проблему конкурентных оплат._
Потому что транзкция работает со снапшотом, который был зафиксирован внутри транзакции на момент ее начала

REPEATABLE READ использует snapshot isolation:
1. Транзакция видит снапшот данных на момент начала (внутри транзации повторные чтения не будут меняться)
2. Это означает, что логика становится более предсказуемой
3. Однако, без блокировок две транзакции параллельно взаимодействуя с одной строкой могут попытаться изменить ее, поэтому тут нужна блокировка, чтобы второй поток не мог одновременно принять решение насчет судьбы этой строки.

### Зачем нужен FOR UPDATE?

Чтобы заблокировать строку до завершения транзакции и не дать другим захватить ее.
Это надо чтобы не просто прочитать статус, а гарантировать что никто другой не изменит до завершения транзакции.

`FOR UPDATE` создает эксклюзивную блокировку на уровне строки (row-level lock):

**Без FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...';
-- Другая транзакция может прочитать ту же строку
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**С FOR UPDATE:**
```sql
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...' FOR UPDATE;
-- Другая транзакция ЖДЕТ освобождения блокировки
UPDATE orders SET status = 'paid' WHERE id = '...';
COMMIT;
```

**Типы блокировок:**
- `FOR UPDATE` — эксклюзивная блокировка (никто не может изменить или заблокировать)
- `FOR SHARE` — разделяемая блокировка (другие могут читать с FOR SHARE, но не могут изменять)
- `FOR NO KEY UPDATE` — как FOR UPDATE, но разрешает concurrent FOR KEY SHARE
- `FOR KEY SHARE` — слабая блокировка (предотвращает DELETE и UPDATE ключевых полей)

### Что произойдет без FOR UPDATE на REPEATABLE READ?

_TODO: Опишите сценарий, когда REPEATABLE READ без FOR UPDATE не работает._
Две параллельные транзакции могут прочитать один и тот же статус и решить что заказ можно оплатить. REPEATABLE READ делает снапшот данных, но при чтении не блокирует  строку. 

Даже на REPEATABLE READ возможна аномалия:

```sql
-- Сессия 1
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
COMMIT;

-- Сессия 2 (параллельно)
BEGIN ISOLATION LEVEL REPEATABLE READ;
SELECT status FROM orders WHERE id = '...'; -- created (snapshot!)
-- Задержка...
UPDATE orders SET status = 'paid' WHERE id = '...' AND status = 'created';
-- ЧТО ПРОИЗОЙДЕТ?
```

_TODO: Объясните, что произойдет в этом случае._
Каждая транзакция работает со своим снапшотом и просто SELECT не блокирует строку.
Первая транзакция делает update + commit => статус "оплачен"
Вторая транзакция тоже пытается сделать это но тут вылезет ошибка could not serialize access due to concurrent update. 
Вторая транзакция откатывается. Но в этом коде ошибка возникнет на моменте обновления данных, а значит действий было больше и это почти самый конец.



### Разница между FOR UPDATE и FOR SHARE

| Характеристика | FOR UPDATE | FOR SHARE |
|----------------|------------|-----------|
| Тип блокировки | Эксклюзивная | Разделяемая |
| Блокирует чтение с FOR UPDATE | ✅ | ✅ |
| Блокирует чтение с FOR SHARE | ✅ | ❌ |
| Блокирует UPDATE | ✅ | ✅ |
| Блокирует DELETE | ✅ | ✅ |
| Use case | Перед изменением | Защита от изменений |

**Пример использования FOR SHARE:**
```sql
-- Проверка наличия товара перед созданием заказа
SELECT quantity FROM products WHERE id = '...' FOR SHARE;
-- Товар не может быть удален или изменен до COMMIT
```

---

## Раздел 4: Рекомендации для продакшена

### Какой ISOLATION LEVEL использовать для продакшена маркетплейса?

**Рекомендация:** Для продакшена маркетплейса рекомендуется использовать **READ COMMITTED как default уровень** с явным использованием **REPEATABLE READ + FOR UPDATE для критичных операций**.

#### Обоснование:

**1. Производительность**
- READ COMMITTED имеет минимальный overhead - не требует отслеживания фантомных записей
- REPEATABLE READ используется только там, где необдходимо: платежные операции, резервирование, фиксация баланса
- Избегаем глобального использования SERIALIZABLE, тк может привести к частым откатам.

**2. Безопасность данных**
- Критичные операции (оплата, изменение баланса) защищены через REPEATABLE READ + FOR UPDATE: одна транзакция не меняет данные.
- Некритичные операции (просмотр каталога) работают на READ COMMITTED тк аномалии не влияют на корректность логики
- Четкое разделение зон ответственности: надо определить уровни изоляци там где необходимо

_TODO: Расширьте обоснование._

**3. Риски deadlock**
- FOR UPDATE может привести к deadlock при неправильном порядке блокировок. Например если сессия 1 сначала далет блокировку заказа А и потом Б. Другая сесстя делает наоборот
- Решение: всегда блокировать ресурсы в одном порядке и не держать блокировки сликгом долго, плюс избегать лишней логики внутри транзакции
- Пример: сначала order, потом order_items, потом payment

_TODO: Расширьте обоснование._

**4. Простота разработки**
- READ COMMITTED легко понять и отлаживать.  У каждого Select-а актуальные данные
- REPEATABLE READ + FOR UPDATE требует explicit intent. Здесь указание на то, что эти данные хотят изменения разработчиком
- Код становится самодокументируемым

_TODO: Расширьте обоснование._

**5. Масштабируемость**
- READ COMMITTED хорошо масштабируется тк не делает много блокировок
- Локальное использование блокировок не создает bottleneck, тк блокируется только одна строка а не вся таблица
- Connection pooling работает эффективно потому что транзации короткие и пул быстрее может переиспользоваться

_TODO: Расширьте обоснование._

### Альтернативные подходы

#### Подход 1: Использовать SERIALIZABLE везде

**Плюсы:**
- Максимальная корректность
- Не нужно думать о блокировках
- PostgreSQL автоматически обнаруживает конфликты

**Минусы:**
- Значительное снижение производительности (20-50%)
- Требует retry logic во всех операциях
- Высокий процент rollback при нагрузке
- Сложная отладка serialization failures

**Вывод:** Не рекомендуется для high-load систем.

#### Подход 2: Optimistic Locking (версионирование)

**Реализация:**
```sql
ALTER TABLE orders ADD COLUMN version INTEGER DEFAULT 1;

UPDATE orders 
SET status = 'paid', version = version + 1
WHERE id = '...' AND status = 'created' AND version = 1;

-- Проверить ROW_COUNT, если 0 — конфликт
```

**Плюсы:**
- Нет блокировок на чтение
- Хорошая производительность
- Масштабируется горизонтально

**Минусы:**
- Требует изменения схемы БД
- Требует retry logic в приложении
- Может быть много конфликтов при высокой нагрузке

**Вывод:** Хороший подход для распределенных систем.

#### Подход 3: Advisory Locks

**Реализация:**
```sql
BEGIN;
SELECT pg_advisory_xact_lock(hashtext('order_' || order_id));
-- Критическая секция
COMMIT; -- Блокировка автоматически снимается
```

**Плюсы:**
- Гибкий контроль блокировок
- Можно блокировать логические ресурсы
- Работает на любом уровне изоляции

**Минусы:**
- Легко забыть снять блокировку
- Сложнее отлаживать
- Требует дисциплины от разработчиков

**Вывод:** Полезно для специфичных случаев, но не как основной подход.

### Итоговая рекомендация

Для продакшена маркетплейса используйте **гибридный подход**:

1. **Default isolation level:** `READ COMMITTED`
   - Для обычных операций (чтение каталога, просмотр истории)
   - 95% операций

2. **REPEATABLE READ + FOR UPDATE:** для критичных операций
   - Оплата заказа
   - Изменение баланса
   - Резервирование товара
   - 5% операций

3. **Дополнительно:** Optimistic locking для операций с высокой конкурентностью
   - Обновление счетчиков
   - Изменение рейтингов

**Пример кода:**
```python
async def pay_order(order_id: UUID):
    async with db.transaction(isolation='repeatable_read'):
        # Заблокировать заказ
        order = await db.fetch_one(
            "SELECT * FROM orders WHERE id = $1 FOR UPDATE",
            order_id
        )
        
        if order['status'] != 'created':
            raise OrderAlreadyPaidError()
        
        # Обновить статус
        await db.execute(
            "UPDATE orders SET status = 'paid' WHERE id = $1",
            order_id
        )
        
        # Записать в историю
        await db.execute(
            "INSERT INTO order_status_history (order_id, status) VALUES ($1, 'paid')",
            order_id
        )
```

---

## Заключение

В ходе лабораторной работы было изучено:
1. Причины возникновения race condition
2. Применение и особенности уровней изоляции транзакций
3. Как применяется REPEATABLE READ + FOR UPDATE для борьбы с race condition и как блокировать строки

Основные выводы:
- REPEATABLE READ + FOR UPDATE защищает от race condition в критичных операциях засчет блокировки строки.
- REPEATABLE READ + FOR UPDATE можно нужно применять в проде для критичных операций в то время как для остальных подойдет READ COMMITED (как раз те где не важна целостность состояния)
- READ COMMITED не защищает от race condition, если решение принимать на основе SELECT-а.

---

## Приложение: Результаты тестирования

### Тест 1: Демонстрация проблемы (READ COMMITTED)

```
               id                  |               order_id               | status  |         changed_at         
--------------------------------------+--------------------------------------+---------+---------------------------
 ef1772f5-c803-48d9-b7ea-79b81f6884dd | 3cc6f11c-58c5-4d06-a66e-f1829f86cfcb | created | 2026-03-26 21:08:37.581564
 8c8bfcec-7189-411c-8515-71b0008ef5ce | 3cc6f11c-58c5-4d06-a66e-f1829f86cfcb | paid    | 2026-03-26 21:08:40.017371
 676fc13c-ec17-49c7-a754-53b05f1ec14f | 3cc6f11c-58c5-4d06-a66e-f1829f86cfcb | paid    | 2026-03-26 21:08:40.018296
```

### Тест 2: Решение проблемы (REPEATABLE READ + FOR UPDATE)


```
               id                  |               order_id               | status  |         changed_at         
--------------------------------------+--------------------------------------+---------+----------------------------
 d33951f5-7a93-4057-944f-9610da21fb06 | 382582d4-060c-4253-b1b8-b6afc65a913b | created | 2026-03-26 19:20:51.641015
 add10820-395f-4ac5-a077-4c9a6876c818 | 382582d4-060c-4253-b1b8-b6afc65a913b | paid    | 2026-03-26 19:20:56.065811
```

---
